# Configuration Netlify. Ce fichier vit dans web/ : lors de la création du
# site sur Netlify, régler "Base directory" = "web" (le repo contient aussi
# l'ancienne app Streamlit à la racine).
[build]
  command = "npm run build"
  publish = "dist"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

[[headers]]
  for = "/_astro/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
